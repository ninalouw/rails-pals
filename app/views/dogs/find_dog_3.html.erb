<div class="container">
    <div class="text-center">
        <h1 class="line-header">Dog Profiles</h1>
    </div>
    <section id="dogs">
        <find-dogs :dogs="<%= @dogs %>" inline-template>
            <div v-for="dog in dogs" :key="dog.id">
                {{dog.name}}
            </div>
        </find-dogs>
    <!-- <%#= tag.div id: :dogs, data: { dogs: @dogs.to_json} %> -->
    </section>
</div>